name: custom_llms
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip

  - pip:
      - chromadb>=0.5.0
      - pypdf>=4.0.0
      - ollama>=0.3.0

      - requests>=2.31.0
      - tqdm>=4.66.0